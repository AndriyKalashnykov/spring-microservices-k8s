{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:best-practices",
    ":semanticCommitType(chore)"
  ],
  "enabledManagers": ["maven", "github-actions", "dockerfile", "kubernetes", "custom.regex"],
  "kubernetes": {
    "managerFilePatterns": ["/^k8s/.+\\.ya?ml$/"]
  },
  "ignorePaths": [
    "**/node_modules/**",
    "**/vendor/**",
    "**/dist/**",
    "**/bin/**",
    "**/obj/**",
    "**/target/**",
    "**/.idea/**",
    "**/.vscode/**"
  ],
  "commitMessagePrefix": "chore(all): ",
  "commitMessageAction": "update",
  "commitBody": "Signed-off-by: Renovate Bot <bot@renovateapp.com>",
  "labels": ["dependencies"],
  "prConcurrentLimit": 50,
  "prHourlyLimit": 0,
  "platformAutomerge": false,
  "automerge": true,
  "automergeType": "pr",
  "automergeStrategy": "squash",
  "ignoreTests": false,
  "customManagers": [
    {
      "customType": "regex",
      "description": "Update Makefile tool versions via inline renovate comments",
      "managerFilePatterns": ["/^Makefile$/"],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>[^\\s]+) depName=(?<depName>[^\\s]+)( versioning=(?<versioning>[^\\s]+))?( extractVersion=(?<extractVersion>[^\\s]+))?\\n[A-Z0-9_]+\\s*:=\\s*(?<currentValue>[^\\s]+)"
      ]
    },
    {
      "customType": "regex",
      "description": "Update .mise.toml tool pins via inline renovate comments",
      "managerFilePatterns": ["/^\\.mise\\.toml$/"],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>[^\\s]+) depName=(?<depName>[^\\s]+)( extractVersion=(?<extractVersion>[^\\s]+))?( versioning=(?<versioning>[^\\s]+))?\\n[a-zA-Z0-9_-]+\\s*=\\s*\"(?<currentValue>[^\"]+)\""
      ]
    },
    {
      "customType": "regex",
      "description": "Track Testcontainers MongoDBContainer image versions in ANY Java file (glob is intentionally broad — renaming a test file should not silently drop Renovate tracking)",
      "managerFilePatterns": ["/\\.java$/"],
      "matchStrings": [
        "new\\s+MongoDBContainer\\(\\s*\"(?<depName>[^:\"]+):(?<currentValue>[^\"]+)\"\\s*\\)"
      ],
      "datasourceTemplate": "docker"
    },
    {
      "customType": "regex",
      "description": "Track mongo image version in docs/reference-architecture.md fenced YAML examples (kept in sync with k8s/mongodb-deployment.yaml)",
      "managerFilePatterns": ["/^docs/.+\\.md$/"],
      "matchStrings": [
        "image:\\s+(?<depName>mongo):(?<currentValue>[0-9]+\\.[0-9]+\\.[0-9]+)"
      ],
      "datasourceTemplate": "docker"
    },
    {
      "customType": "regex",
      "description": "Track container-image tags pinned in GitHub Actions workflow `run:` steps via inline `# renovate:` comments on shell `VAR=\"tag\"` assignments. Currently the container-structure-test image in ci.yml's image-scan job (gcr.io/gcp-runtimes/container-structure-test, frozen upstream at v1.16.0; kept in lockstep with the Makefile's CONTAINER_STRUCTURE_TEST_VERSION). datasource=docker — the image registry — never github-releases, because the gcr.io image is frozen while the GitHub binary releases continue past it.",
      "managerFilePatterns": ["/^\\.github/workflows/.+\\.ya?ml$/"],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>[^\\s]+) depName=(?<depName>[^\\s]+)(?: versioning=(?<versioning>[^\\s]+))?(?: extractVersion=(?<extractVersion>[^\\s]+))?\\s+[A-Z0-9_]+=\"(?<currentValue>[^\"]+)\""
      ]
    },
    {
      "customType": "regex",
      "description": "Track the pinned C4-PlantUML stdlib version in the !include URLs across docs/diagrams/*.puml. One bump PR edits every occurrence. AUTOMERGE DISABLED (see packageRule): a stdlib bump requires regenerating the committed PNGs (make diagrams), which hosted Renovate cannot do — diagrams-check will go RED until a maintainer regenerates + commits the PNGs.",
      "managerFilePatterns": ["/^docs/diagrams/.+\\.puml$/"],
      "matchStrings": ["plantuml-stdlib/C4-PlantUML/(?<currentValue>v[0-9][^/]*)/"],
      "datasourceTemplate": "github-tags",
      "depNameTemplate": "plantuml-stdlib/C4-PlantUML"
    }
  ],
  "vulnerabilityAlerts": {
    "labels": ["security"],
    "automerge": true,
    "minimumReleaseAge": "0 days"
  },
  "packageRules": [
    {
      "description": "Wait 3 days before automerging major updates",
      "matchUpdateTypes": ["major"],
      "minimumReleaseAge": "3 days"
    },
    {
      "description": "Regex-managed docker refs (Makefile PlantUML/mermaid-cli/cloud-provider-kind/container-structure-test, docs mongo, Testcontainers, workflow container-structure-test) don't capture currentDigest — skip digest pinning to avoid Renovate 'update failure' on pin-dependencies branch",
      "matchManagers": ["custom.regex"],
      "matchDatasources": ["docker"],
      "pinDigests": false
    },
    {
      "description": "Group GitHub Actions into one PR",
      "matchManagers": ["github-actions"],
      "groupName": "GitHub Actions"
    },
    {
      "description": "Group Dockerfile base image updates",
      "matchManagers": ["dockerfile"],
      "groupName": "Docker base images"
    },
    {
      "description": "Group Spring Boot + Spring Framework + Spring Cloud into ONE PR. These release trains are version-locked: Spring Cloud enforces a runtime CompatibilityVerifier against the Spring Boot version (e.g. Spring Cloud 2025.1.x supports Boot 4.0.x; 2025.1.2 / commons 5.0.2 added 4.1.x), and Spring Framework is governed by the Boot BOM. Bumping them in separate PRs yields individually-unmergeable PRs — a Boot-only bump fails the runtime verifier, a Cloud-only bump fails to compile. One group bumps them together so they pass/fail as a unit (see CLAUDE.md backlog #4).",
      "matchPackageNames": [
        "/^org\\.springframework\\.boot/",
        "/^org\\.springframework:/",
        "/^org\\.springframework\\.cloud/"
      ],
      "groupName": "Spring"
    },
    {
      "description": "Group Testcontainers dependencies",
      "matchPackageNames": ["/^org\\.testcontainers/"],
      "groupName": "Testcontainers"
    },
    {
      "description": "Group Micrometer dependencies",
      "matchPackageNames": ["/^io\\.micrometer/"],
      "groupName": "Micrometer"
    },
    {
      "description": "Group BouncyCastle dependencies (bcpkix/bcutil/bcprov pinned together to address transitive CVEs from io.kubernetes:client-java; keep all three on the same version)",
      "matchPackageNames": ["/^org\\.bouncycastle:/"],
      "groupName": "BouncyCastle"
    },
    {
      "description": "Group the mongo image bump across the k8s manifest (kubernetes manager) and the Testcontainers + docs regex managers into one PR so the version stays consistent everywhere. Constrained to the 8.0 LTS line (EOL 2029-10-31) via allowedVersions: Renovate lands 8.0.x security patches but never jumps to a short-lived rapid-release line (8.1/8.2/8.3/8.4). See CLAUDE.md upgrade backlog #5.",
      "matchDatasources": ["docker"],
      "matchPackageNames": ["mongo"],
      "groupName": "MongoDB",
      "allowedVersions": "/^8\\.0\\./"
    },
    {
      "description": "Lock kubectl (.mise.toml) to the cluster node's minor line (kindest/node v1.35.5 -> kubectl 1.35.x) for an exact apiserver match. The e2e job's paths-filter excludes .mise.toml, so a kubectl-only bump never runs e2e and CI cannot catch kubectl<->cluster skew drift — this constraint is the guard. Bump it together with the kind CLI + kindest/node when the cluster moves to a new minor.",
      "matchPackageNames": ["kubernetes/kubernetes"],
      "allowedVersions": "/^1\\.35\\./"
    },
    {
      "description": "Lock the eclipse-temurin base image to the Java 25 major. The Dockerfile temurin pin is the only Renovate-tracked Java coordinate (poms/.mise.toml/.java-version are major-only and manually bumped), so without this lock a 25->26 bump would land CI-green (Java-25 bytecode runs on a JRE-26 base) while the rest of the toolchain stays 25 — a silent runtime/compile-target split. The Java major is a deliberate coordinated event (see CLAUDE.md backlog #3); bump this allowedVersions together with poms/.mise.toml/.java-version. check-toolchain-alignment now also guards the Dockerfile<->pom major.",
      "matchDatasources": ["docker"],
      "matchPackageNames": ["eclipse-temurin"],
      "allowedVersions": "/^25\\./"
    },
    {
      "description": "Do not automerge the CVE-override pins (BouncyCastle) — these sit in <dependencyManagement> as a deliberate CVE-fix floor and must be reviewed by hand and removed once the Spring Boot BOM catches up (see CLAUDE.md upgrade backlog). jackson-core override removed once the Boot 4.1.0 BOM reached 3.1.4 (>= the GHSA-2m67-wjpj-xhg9 fix floor); tomcat-embed-core override removed once the Boot 4.1.0 BOM reached 11.0.22 (CVE-2026-34483/34486/34487 fix floor).",
      "matchPackageNames": [
        "/^org\\.bouncycastle:/"
      ],
      "automerge": false
    },
    {
      "description": "Hold .mise.toml tool bumps 3 days so the upstream release artifact that mise installs is published before the PR opens (tag-before-release-publish trap; partial buffer — the real backstop is the required ci-pass check). Scoped to the custom.regex manager + .mise.toml file because the mise manager is NOT enabled (.mise.toml is tracked via custom.regex); a matchManagers:[\"mise\"] rule would be inert config.",
      "matchManagers": ["custom.regex"],
      "matchFileNames": [".mise.toml"],
      "minimumReleaseAge": "3 days"
    },
    {
      "description": "Do not automerge C4-PlantUML stdlib bumps — they require manual `make diagrams` PNG regeneration before diagrams-check passes.",
      "matchDepNames": ["plantuml-stdlib/C4-PlantUML"],
      "automerge": false
    },
    {
      "description": "Label Java packages",
      "matchCategories": ["java"],
      "addLabels": ["lang: java"]
    }
  ]
}
