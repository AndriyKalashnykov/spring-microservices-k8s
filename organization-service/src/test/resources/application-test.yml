spring:
  config:
    import: ""
  cloud:
    kubernetes:
      enabled: false
      config:
        enabled: false
      discovery:
        enabled: false

# Disable SpringDoc endpoints in tests to keep output clean (enabled by default in main profile).
springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false
