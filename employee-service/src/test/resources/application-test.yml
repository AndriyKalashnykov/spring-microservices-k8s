spring:
  config:
    import: ""
  cloud:
    kubernetes:
      enabled: false
      config:
        enabled: false
      discovery:
        enabled: false

# Disable SpringDoc endpoints in tests to keep output clean (enabled by default in main profile).
springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false

# Disable OTLP exporters in tests — no collector to reach.
management:
  otlp:
    tracing:
      export:
        enabled: false
    metrics:
      export:
        enabled: false
